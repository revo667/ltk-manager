import {
  BLEND_MODE,
  COLOR_LOOKUP,
  LINGER_TYPE,
  QUAD_TYPE,
  STENCIL_MODE,
  UV_MODE,
} from "../../model/enums";
import { type EmitterModel, plainUvLayer, POINT_SHAPE, type ValueCurve } from "../../model/model";

/** A curve holding `constant` and no keys. */
export function flat(...constant: number[]): ValueCurve {
  return { constant, keys: [], tables: [] };
}

/** An emitter of the opened system at `index`, spawning nothing, with `over` written on it. */
export function emitterOf(index: number, over: Partial<EmitterModel> = {}): EmitterModel {
  return {
    customMaterial: null,
    index,
    simple: false,
    listIndex: index,
    name: `spark ${index}`,
    disabled: false,
    culled: null,
    rate: flat(0),
    particleLifetime: flat(100),
    lifetime: null,
    timeBeforeFirstEmission: 0,
    singleParticle: false,
    sharedRandom: false,
    birthVelocity: flat(0, 0, 0),
    acceleration: flat(0, 0, 0),
    drag: flat(0, 0, 0),
    birthDrag: flat(0, 0, 0),
    velocity: flat(0, 0, 0),
    worldAcceleration: flat(0, 0, 0),
    birthOrbitalVelocity: flat(0, 0, 0),
    bindWeight: flat(0),
    emitterPosition: flat(0, 0, 0),
    emitterSpace: false,
    shape: POINT_SHAPE,
    emissionSurface: null,
    rotationOverride: [0, 0, 0],
    scaleOverride: [1, 1, 1],
    translationOverride: [0, 0, 0],
    localOrientation: true,
    particleLocalOrientation: false,
    uniformScale: false,
    particleLinger: 0,
    emitterLinger: 0,
    lingerType: LINGER_TYPE.maxLifetimeAfterEmitterDies,
    linger: null,
    palette: null,
    erosion: null,
    distortion: null,
    reflection: null,
    soft: null,
    lookupX: COLOR_LOOKUP.lifetime,
    lookupY: COLOR_LOOKUP.constant,
    lookupOffsets: [0, 0],
    lookupScales: [1, 1],
    colorTexture: null,
    uv: plainUvLayer(),
    uvMode: UV_MODE.default,
    multTexture: null,
    multUv: null,
    rotation0: flat(0, 0, 0),
    birthRotation0: flat(0, 0, 0),
    birthRotationalVelocity0: flat(0, 0, 0),
    birthRotationalAcceleration: flat(0, 0, 0),
    legacySimple: null,
    pivotUp: false,
    rotationEnabled: false,
    directionOriented: false,
    directionVelocityScale: 0,
    directionVelocityMinScale: 1,
    scale0: flat(1, 1, 1),
    birthScale0: flat(1, 1, 1),
    color: flat(1, 1, 1, 1),
    birthColor: flat(1, 1, 1, 1),
    texture: null,
    blendMode: BLEND_MODE.add,
    pass: 0,
    miscRenderFlags: 0,
    groundLayer: false,
    alphaRef: 0,
    quadType: QUAD_TYPE.cameraQuad,
    stencilMode: STENCIL_MODE.disabled,
    stencilRef: 0,
    primitiveClass: null,
    primitiveName: null,
    mesh: null,
    trail: null,
    beam: null,
    childSet: null,
    fields: null,
    depthBias: [0, 0],
    backfaceCull: true,
    depthPushPull: 0,
    ...over,
  };
}
