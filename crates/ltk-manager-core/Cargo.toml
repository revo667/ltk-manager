[package]
name = "ltk-manager-core"
version = "0.1.0"
description = "UI-agnostic core library for LTK Manager"
edition = "2024"
license = "GPL-3.0-or-later"
repository = "https://github.com/LeagueToolkit/ltk-manager"
publish = false

[features]
# Derive `ts_rs::TS` and `specta::Type` on core types so the Tauri shell can
# export bindings for types that cross IPC (e.g. `Config` flattened into
# `Settings`). The two exporters run side by side while the command table moves
# onto `tauri-specta` one module at a time.
# The `ts-rs` impl features belong with the derive rather than with the shell
# that consumes it, so this crate builds and tests on its own.
ts = [
    "dep:ts-rs",
    "ts-rs/chrono-impl",
    "ts-rs/indexmap-impl",
    "dep:specta",
    "ltk-meta-docs/ts",
]

[dependencies]
filetime = { workspace = true }
ritoclient = { workspace = true }

ltk-declarations = { workspace = true }
ltk-meta-docs = { workspace = true }
ltk_anim = { workspace = true }
ltk_fantome = { workspace = true }
ltk_file = { workspace = true }
ltk_game_data = { workspace = true }
ltk_hash = { workspace = true }
ltk_hashdb = { workspace = true }
ltk_hashtable = { workspace = true }
ltk_mapgeo = { workspace = true }
ltk_mesh = { workspace = true }
ltk_meta = { workspace = true }
ltk_mimir_cache = { workspace = true }
ltk_modpkg = { workspace = true }
ltk_mod_project = { workspace = true }
ltk_overlay = { workspace = true }
ltk_rst = { workspace = true }
ltk_texture = { workspace = true }
ltk_wad = { workspace = true }
ltk-telemetry = { workspace = true }

base64 = { workspace = true }
brex = { workspace = true }
camino = { workspace = true }
chrono = { workspace = true }
flate2 = { workspace = true }
fs-err = { workspace = true }
glam = { workspace = true }
image = { workspace = true }
image_dds = { workspace = true }
indexmap = { workspace = true }
lru = { workspace = true }
parking_lot = { workspace = true }
rayon = { workspace = true }
regex = { workspace = true }
reqwest = { workspace = true }
rmp-serde = { workspace = true }
semver = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml_ng = { workspace = true }
sha2 = { workspace = true }
slug = { workspace = true }
specta = { workspace = true, optional = true }
strum = { workspace = true }
tar = { workspace = true }
tempfile = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
ts-rs = { workspace = true, optional = true }
uuid = { workspace = true }
walkdir = { workspace = true }
webp = { workspace = true }
zip = { workspace = true }

[target.'cfg(windows)'.dependencies]
winreg = "0.55"
windows-sys = { workspace = true, features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Security_Cryptography",
    "Win32_Security_WinTrust",
    "Win32_Storage_FileSystem",
    "Win32_System_Diagnostics_ToolHelp",
    "Win32_System_Ioctl",
    "Win32_System_IO",
    "Win32_System_Registry",
    "Win32_System_Threading",
    "Win32_UI_WindowsAndMessaging",
] }

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2"

[dev-dependencies]
assert_matches = { workspace = true }
